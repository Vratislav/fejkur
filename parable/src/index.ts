console.log("Hello, TypeScript Node.js!");

// Example function to demonstrate TypeScript
function greet(name: string): string {
  return `Hello, ${name}!`;
}

const message = greet("Worldd");
console.log(message);
